# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_USER=user1
DB_NAME=skills_db
DB_PASS=changeme_in_production

# JWT Configuration
JWT_SECRET=changeme_to_secure_random_value
JWT_EXPIRES=1h

# Frontend Configuration
NUXT_PUBLIC_API_BASE=http://localhost:7000

# Optional: CORS Configuration (comma-separated origins, or * for development)
CORS_ORIGIN=http://localhost:3000,https://your-production-domain.com